from pathlib import Path
p=Path('README.md');s=p.read_text();needle='Latest execution work: ';a=s.index(needle);b=s.index('\n\n',a);s=s[:a]+'Latest execution work: [GPU-resident expert decode and CUDA graph replay](GPU_RESIDENT_EXPERTS.md) keeps single-token GPU-only expert activations, weights and merging on device. Correctness, cancellation, cache lifecycle and CUDA replay checks pass. The boundary-only matched benchmark measured 17.906 versus 18.727 tokens/s; see the report for the separate replay comparison.'+s[b:];p.write_text(s)
p=Path('FREETOKEN_EXECUTION_GAP_PLAN.md');s=p.read_text();a=s.index('Implementation update:');b=s.index('\n\n',a);s=s[:a]+'Implementation update: persistent cached-GPU workspaces, the single-token GPU-resident boundary and verified CUDA replay are now implemented. See [GPU-resident execution and measured results](GPU_RESIDENT_EXPERTS.md) and the earlier [persistent-workspace results](PERSISTENT_EXPERT_WORKSPACE.md). Multi-token device execution and the later CPU/transfer/kernel stages remain pending. The original design below is retained.'+s[b:];p.write_text(s)
p=Path('PERSISTENT_EXPERT_WORKSPACE.md');s=p.read_text().replace('Implemented September 9, 2026 as the first slice of `FREETOKEN_EXECUTION_GAP_PLAN.md`.','Implemented September 9, 2026 as the first slice of `FREETOKEN_EXECUTION_GAP_PLAN.md`.\n\nFollow-up: [GPU-resident decode and CUDA graph replay](GPU_RESIDENT_EXPERTS.md) implements and validates the next slice. The report below describes the earlier workspace-only build.');p.write_text(s)
